<h3><%= @career.name %></h3>
#DESCRIPTION
<br><br>

<% if !!current_user.characters.find_by(career_id: params[:id]) %>
    <%= button_to "My #{@career.name} Characters", career_characters_path(@career) %>
<% else %>
    <%= link_to "Add a #{@career.name} Character", new_career_character_path(@career) %>
<% end %>
