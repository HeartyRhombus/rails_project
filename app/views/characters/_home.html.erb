<% if user_signed_in? %>
<ul class="list-group">
    <% @characters.order(:name).each do |c| %>
        <%= link_to "#{c.name} - Level: #{c.level}", c, class: "list-group-item list-group-item-action btn btn-outline-info" %>
    <% end %>
</ul>
    <br> <br>

<% end %>